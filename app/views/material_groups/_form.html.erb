<%= simple_form_for(@material_group, :html => {:class=> "form-horizontal"}) do |f| %>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.button :submit, :class=> "btn btn-success" %>
  </div>
<% end %>
